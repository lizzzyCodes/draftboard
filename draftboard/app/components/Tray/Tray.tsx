//Managing a tray/bucket state that allows selection of exactly 2 players to display side-by-side with a glowing halo effect matching their team colors.

import PrimaryButton from "../Buttons/PrimaryButton";

export default function Tray() {
  return <PrimaryButton text={"COMPARE PLAYERS"} />;
}
